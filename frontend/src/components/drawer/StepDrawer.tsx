/**
 * StepDrawer — 计划步骤详情（Plan C Epic 6 Task 22）。
 * Task 20 先建空壳,Task 22 填实。
 */
export function StepDrawer(_props: {
  id: string
  hasBack: boolean
  onBack: () => void
  onClose: () => void
}) {
  return null
}

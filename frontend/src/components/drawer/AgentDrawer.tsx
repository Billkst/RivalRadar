/**
 * AgentDrawer — agent 详情 + 技能管理(Plan C Epic 6 Task 21）。
 * Task 20 先建空壳,Task 21 填实。
 */
import type { AgentId } from '@/types/agents'

export function AgentDrawer(_props: {
  role: AgentId
  hasBack: boolean
  onBack: () => void
  onClose: () => void
}) {
  return null
}
